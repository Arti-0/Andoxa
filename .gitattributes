# Image assets are committed as regular Git blobs (NOT Git LFS): they are served
# from /public by the app, and Vercel ships LFS *pointer files* instead of the
# binaries, which broke every PNG/JPG in production. Keep raster assets out of LFS.
# Only large, non-served archives stay in LFS.
*.zip filter=lfs diff=lfs merge=lfs -text
